events {}

http {
  upstream backend_service {
    # Round-robin is DEFAULT
    server backend:3000;
    server backend:3000;
  }

  server {
    listen 80;

    location /api {
      proxy_pass http://backend_service;
    }
  }
}
